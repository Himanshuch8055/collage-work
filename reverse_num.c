#include <stdio.h>

int main() 
{
    int a, b;

    for ( a = 0; a <= 10; a++)
    {
        printf("%d", a);
    }

    printf("\n");

    for ( b = 10; b > 0; b--)
    {
        printf("%d", b);
    }
    
    

    return 0;
}