#include <stdio.h>

int main() {

   // int num1;
   // int num2;

   // printf("Enter the num1: ");
   // scanf("%d", &num1);
   // printf("Enter the num2: ");
   // scanf("%d", &num2);


   int even;
   int odd;

   printf("Enter the NUM: ");
   scanf("%d", &even);

   printf("Enter the NUM: ");
   scanf("%d", &odd);
   
   // if ((num1 % 2 == 0) || (num2 % 2 == 0))
   //    printf("%d is even\n", num1, num2);

   // if ((num1 / 2 != 0) || (num2 / 2 != 0))
   //    printf("%d is odd\n", num1, num2);

   return 0;
}